using Common.Messaging;
using GameInterface.Utils.NetworkEvents;
using ProtoBuf;
using System.Collections.Generic;
using System.Reflection;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace AutoSync
{
    [ProtoContract(SkipConstructor = true)]
    public record {{ MemberDeclaringTypeName}}_{{ MemberName }}_RemoveNetworkMessage : GenericNetworkEvent<{{ MemberDeclaringType}}, {{ KeyType }}>
    {
        [ProtoMember(1)]
        public override string InstanceId { get; set; }

        [ProtoMember(2)]
        public {{ KeyWireType }} Key { get; set; }

        public {{ MemberDeclaringTypeName}}_{{ MemberName }}_RemoveNetworkMessage(string instanceId, {{ KeyWireType }} key) : base(instanceId)
        {
            Key = key;
        }
    }
}
