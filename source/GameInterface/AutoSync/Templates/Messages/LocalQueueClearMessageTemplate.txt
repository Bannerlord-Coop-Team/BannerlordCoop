using Common.Messaging;
using GameInterface.Utils.LocalEvents;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}
    
public readonly struct Local_{{ MemberDeclaringTypeName}}_{{ MemberName }}_QueueClear : IEvent
{
    public readonly {{ MemberDeclaringType }} Instance;

    public Local_{{ MemberDeclaringTypeName}}_{{ MemberName }}_QueueClear({{ MemberDeclaringType }} instance)
    {
        Instance = instance;
    }
}