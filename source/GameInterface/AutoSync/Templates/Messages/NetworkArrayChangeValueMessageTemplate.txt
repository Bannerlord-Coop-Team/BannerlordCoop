using Common.Messaging;
using GameInterface.Utils.NetworkEvents;
using ProtoBuf;
using System.Collections.Generic;
using System.Reflection;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace AutoSync
{
    [ProtoContract(SkipConstructor = true)]
    public record {{ MemberDeclaringTypeName}}_{{ MemberName }}_ChangeNetworkMessage : GenericNetworkEvent<{{ MemberDeclaringType}}, {{ ElementType }}>
    {
        [ProtoMember(1)]
        public override uint InstanceId { get; set; }

        [ProtoMember(2)]
        public byte[] Value { get; set; }
        
        [ProtoMember(3)]
        public int Index { get; set; }

        public {{ MemberDeclaringTypeName}}_{{ MemberName }}_ChangeNetworkMessage(uint instanceId, byte[] value, int index) : base(instanceId)
        {
            Index = index;
            Value = value;
        }
    }
}
