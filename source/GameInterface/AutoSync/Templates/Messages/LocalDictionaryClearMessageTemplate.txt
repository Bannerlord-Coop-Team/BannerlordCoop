using Common.Messaging;
using GameInterface.Utils.LocalEvents;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace AutoSync;

public readonly struct Local_{{ MemberDeclaringTypeName}}_{{ MemberName }}_DictionaryClear : IEvent
{
    public readonly {{ MemberDeclaringType }} Instance;

    public Local_{{ MemberDeclaringTypeName}}_{{ MemberName }}_DictionaryClear({{ MemberDeclaringType }} instance)
    {
        Instance = instance;
    }
}
