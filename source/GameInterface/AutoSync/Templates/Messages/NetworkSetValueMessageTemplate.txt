using Common.Messaging;
using GameInterface.Utils.NetworkEvents;
using ProtoBuf;
using System.Collections.Generic;
using System.Reflection;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace AutoSync
{
    [ProtoContract(SkipConstructor = true)]
    public record {{ MemberDeclaringTypeName}}_{{ MemberIdentifier }}_SetNetworkMessage : GenericNetworkEvent<{{ MemberDeclaringType}}, byte[]>
    {
        [ProtoMember(1)]
        public override string InstanceId { get; set; }

        [ProtoMember(2)]
        public byte[] Value { get; set; }

        public {{ MemberDeclaringTypeName}}_{{ MemberIdentifier }}_SetNetworkMessage(string instanceId, byte[] value) : base(instanceId)
        {
            Value = value;
        }
    }
}
