using Common.Messaging;
using GameInterface.Utils.NetworkEvents;
using ProtoBuf;
using System.Collections.Generic;
using System.Reflection;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace AutoSync
{
    [ProtoContract(SkipConstructor = true)]
    public record {{ MemberDeclaringTypeName}}_{{ MemberName }}_AddNetworkMessage : GenericNetworkEvent<{{ MemberDeclaringType}}, {{ ElementType }}>
    {
        [ProtoMember(1)]
        public override uint InstanceId { get; set; }

        [ProtoMember(2)]
        public byte[] Value { get; set; }

        public {{ MemberDeclaringTypeName}}_{{ MemberName }}_AddNetworkMessage(uint instanceId, byte[] value) : base(instanceId)
        {
            Value = value;
        }
    }
}
