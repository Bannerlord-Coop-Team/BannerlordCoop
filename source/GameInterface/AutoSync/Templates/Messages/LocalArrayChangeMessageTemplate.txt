using GameInterface.Utils.LocalEvents;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace AutoSync
{
    public record {{ MemberDeclaringType}}_{{ MemberName }}_ChangeLocalMessage : GenericArrayChangedEvent<{{ MemberDeclaringType }}, {{ ElementType }}>
    {
        public {{ MemberDeclaringType}}_{{ MemberName }}_ChangeLocalMessage({{ MemberDeclaringType }} instance, {{ ElementType }} value, int index) : base(instance, value, index)
        {
        }
    }
}
