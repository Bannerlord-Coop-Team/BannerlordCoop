using Common.Messaging;
using GameInterface.Utils.NetworkEvents;
using ProtoBuf;
using System.Collections.Generic;
using System.Reflection;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace AutoSync
{
    [ProtoContract(SkipConstructor = true)]
    public record {{ MemberDeclaringTypeName}}_{{ MemberName }}_ChangeNetworkMessage : GenericNetworkReferenceEvent<{{ MemberDeclaringType}}, {{ ElementType }}>
    {
        [ProtoMember(1)]
        public override uint InstanceId { get; set; }

        [ProtoMember(2)]
        public override uint ValueId { get; set; }
        
        [ProtoMember(3)]
        public int Index { get; set; }

        public {{ MemberDeclaringTypeName}}_{{ MemberName }}_ChangeNetworkMessage(uint instanceId, uint valueId, int index) : base(instanceId, valueId)
        {
            Index = index;
        }
    }
}
