using Common.Messaging;
using GameInterface.Utils.NetworkEvents;
using ProtoBuf;
using System.Collections.Generic;
using System.Reflection;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace AutoSync
{
    [ProtoContract(SkipConstructor = true)]
    public record {{ MemberDeclaringTypeName}}_{{ MemberName }}_RemoveNetworkMessage : GenericNetworkReferenceEvent<{{ MemberDeclaringType}}, {{ ElementType }}>
    {
        [ProtoMember(1)]
        public override uint InstanceId { get; set; }

        [ProtoMember(2)]
        public override uint ValueId { get; set; }

        public {{ MemberDeclaringTypeName}}_{{ MemberName }}_RemoveNetworkMessage(uint instanceId, uint valueId) : base(instanceId, valueId)
        {
        }
    }
}
