using Common.Messaging;
using GameInterface.Utils.LocalEvents;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace DynamicSync
{
    public record {{ MemberDeclaringType}}_{{ MemberName }}_SetLocalMessage : GenericEvent<{{ MemberDeclaringType }}, {{ ElementType }}>
    {
        public int PropertyValue { get; }

        public {{ MemberDeclaringType}}_{{ MemberName }}_SetLocalMessage({{ MemberDeclaringType }} instance, {{ ElementType }} attribute, int value) : base(instance, attribute)
        {
            PropertyValue = value;
        }
    }
}