using Common.Messaging;
using GameInterface.Utils.NetworkEvents;
using ProtoBuf;
using System.Collections.Generic;
using System.Reflection;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace DynamicSync
{
    [ProtoContract(SkipConstructor = true)]
    public readonly struct NetworkSet_{{ MemberDeclaringType}}_{{ MemberName }}_Message
    {
        [ProtoMember(1)]
        public override string InstanceId { get; }

        [ProtoMember(2)]
        public override string ValueId { get; }

        public {{ MemberDeclaringType}}_{{ MemberName }}_SetNetworkMessage(string instanceId, string valueId)
        {
            InstanceId = instanceId;
            ValueId = valueId;
        }
    }
}
