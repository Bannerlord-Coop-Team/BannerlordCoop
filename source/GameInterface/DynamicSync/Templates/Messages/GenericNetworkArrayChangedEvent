using Common.Messaging;
using GameInterface.Utils.NetworkEvents;
using ProtoBuf;
using System.Collections.Generic;
using System.Reflection;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace DynamicSync
{
    [ProtoContract(SkipConstructor = true)]
    public record {{ MessageType }} : GenericNetworkEvent<{{ MemberDeclaringType}}, {{ MemberType }}>
    {
        [ProtoMember(1)]
        public override string InstanceId { get; set; }

        [ProtoMember(2)]
        public {{ MemberType }} Value { get; set; }

        [ProtoMember(3)]
        public int Index { get; set; }

        public {{ MessageType }}(string instanceId, {{ MemberType }} value, int index) : base(instanceId)
        {
            Value = value;
            Index = index;
        }
    }
}
