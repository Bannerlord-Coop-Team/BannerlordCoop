using Common.Messaging;
using GameInterface.Utils.NetworkEvents;
using ProtoBuf;
using System.Collections.Generic;
using System.Reflection;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace DynamicSync
{
    [ProtoContract(SkipConstructor = true)]
    public record {{ MemberDeclaringType}}_{{ MemberName }}_SetNetworkMessage : ICommand
    {
        [ProtoMember(1)]
        public string InstanceId { get; set; }

        [ProtoMember(2)]
        public string ValueId { get; set; }

        public {{ MemberDeclaringType}}_{{ MemberName }}_SetNetworkMessage(string instanceId, string valueId)
        {
            InstanceId = instanceId;
            ValueId = valueId;
        }
    }
}
