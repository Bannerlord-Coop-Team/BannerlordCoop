using Common.Messaging;
using GameInterface.Utils.LocalEvents;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace DynamicSync
{
    public readonly struct {{ MessageName }} : IEvent
    {
        public readonly {{ MemberDeclaringType }} Instance;
        public readonly {{ MemberType }} Value;

        public {{ MessageName }}({{ MemberDeclaringType }} instance, {{ MemberType }} value)
        {
            Instance = instance;
            Value = value;
        }
    }
}
