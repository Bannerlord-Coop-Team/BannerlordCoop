using Common.Messaging;
using GameInterface.Utils.NetworkEvents;
using ProtoBuf;
using System.Collections.Generic;
using System.Reflection;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace DynamicSync
{
    [ProtoContract(SkipConstructor = true)]
    public record {{ MemberDeclaringType}}_{{ MemberName }}_SetNetworkMessage : GenericNetworkReferenceEvent<{{ MemberDeclaringType}}, {{ MemberType }}>
    {
        [ProtoMember(1)]
        public override string InstanceId { get; set; }

        [ProtoMember(2)]
        public override string ValueId { get; set; }

        [ProtoMember(3)]
        public int ValueTypeIndex { get; set; }


        public {{ MemberDeclaringType}}_{{ MemberName }}_SetNetworkMessage(string instanceId, string valueId, int valueTypeIndex) : base(instanceId, valueId)
        {
            ValueTypeIndex = valueTypeIndex;
        }
    }
}
