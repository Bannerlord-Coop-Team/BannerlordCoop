using Common.Messaging;
using GameInterface.Utils.NetworkEvents;
using ProtoBuf;
using System.Collections.Generic;
using System.Reflection;
{{~ for library in Libraries ~}}
using {{ library }};
{{~ end ~}}

namespace DynamicSync
{
    [ProtoContract(SkipConstructor = true)]
    public readonly struct NetworkSet_{{ MemberDeclaringType }}_{{ MemberName }}Message
    {
        [ProtoMember(1)]
        public override string InstanceId { get; }

        [ProtoMember(2)]
        public {{ MemberType }} Value { get; }

        public {{ MemberDeclaringType}}_{{ MemberName }}_SetNetworkMessage(string instanceId, {{ MemberType }} value)
        {
            InstanceId = instanceId;
            Value = value;
        }
    }
}
