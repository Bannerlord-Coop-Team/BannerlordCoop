using Xunit;

namespace GameInterface.Tests.Serialization.SerializerTests
{
    [CollectionDefinition(nameof(HideoutSerializationCollection), DisableParallelization = true)]
    public class HideoutSerializationCollection
    {
        public const string Name = nameof(HideoutSerializationCollection);
    }
}
